# AI Coursework Candidate Instructions

## Goal
Maintain this as a clean, job-facing coursework candidate showing practical machine learning and AI fundamentals: Naive Bayes text classification, SVM digit classification, search, minimax, and alpha-beta pruning.

## Constraints
- Keep notebooks and scripts honest to the original coursework.
- Do not add private datasets, large generated outputs, local machine paths, caches, or checkpoints.
- Do not inflate the project into production ML; present it as fundamentals plus reproducible validation.
- Do not publish or push without Peter's explicit approval.

## Validation
- Run `python3 tests/smoke_minimax.py`.
- Run `python3 tests/smoke_search.py`.
- Run `python3 tests/validate_notebooks.py`.
- Confirm no `__pycache__`, checkpoints, private paths, or large generated artifacts remain.

## Handoff
Return files changed, checks run, findings, blockers, and next recommended action.
